# Local Netlify folder
.netlify

# macOS
.DS_Store

# Test tool caches / run artifacts (regenerated, not source)
.playwright-cli/
test-results/
appscript/.playwright-cli/
appscript/test-results/

# Local design/finance working files (large binaries, some already
# committed selectively from output/ — see git ls-files output/).
# Not deleted, just kept out of `git status` noise.
tmp/
output/
